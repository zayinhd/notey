import React from "react";

export default function NoteCard() {
    return (
        <textarea
            className="w-full h-full  resize-none whitespace-pre-wrap break-words"
            wrap="soft"
        ></textarea>
    );
}
